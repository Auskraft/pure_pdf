package com.auskraft.purepdf.ui.theme

import androidx.compose.material3.Typography

/**
 * Default Roboto typography per the handoff (it specifies Roboto for UI). Per-element sizes
 * from the design are applied inline at call sites rather than overriding every M3 role.
 */
val PurePdfTypography = Typography()
